#pragma once
#include <vector>

class Nim
{
    private:
    std::vector<int> piles;
    int currentPlayer;

    public:
    //1. board set up
    //2. move/chat/forfeit checks (also move validity check)
    //3. decode/encode moves
    //4. local board update (updates based on whats recieved?)
    //5. game over check
    //6. display board function
    //7. checks for opponent forfeit?
};